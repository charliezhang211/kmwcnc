<?php
/**
 * Template Name: Blog
 *
 * Shell page template for the Blog listing page.
 * WordPress uses this to register the page in the admin.
 *
 * The actual rendering is handled by home.php (WordPress template hierarchy).
 * This file intentionally outputs nothing — it exists only so Page Setup
 * can detect it, create the "Blog" page, and let you assign it as the
 * "Posts page" in Settings → Reading.
 *
 * Setup steps (one-time):
 *  1. Click "Create Page" in Appearance → Page Setup → Blog
 *  2. Go to Settings → Reading
 *  3. Set "Your homepage displays" → "A static page"
 *  4. Posts page → Blog
 *  5. Save — done. WordPress will now use home.php to render /blog/
 *
 * @package ULX
 */
